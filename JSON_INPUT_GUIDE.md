# JSON 输入格式使用指南

## 概述
CLI 现已支持 JSON 格式输入，使数据录入更加规范和高效。

## 功能说明

### 1. 添加城市 (菜单选项 1)

**JSON 格式：**
```json
{"id": "25", "name": "昆明", "x": -1250, "y": -149, "description": "云南省会"}
```

**字段说明：**
- `id`: 城市编号（必需，字符串格式）
- `name`: 城市名称（必需，字符串）
- `x`: X坐标（必需，整数）
- `y`: Y坐标（必需，整数）
- `description`: 城市简介（必需，可为空字符串）

**示例：**
```json
{"id": "101", "name": "北京", "x": 0, "y": 0, "description": "首都"}
{"id": "102", "name": "上海", "x": 500, "y": -300, "description": "经济中心"}
```

---

### 2. 删除城市 (菜单选项 2)

**JSON 格式：**
```json
{"id": "25"}
```

**向后兼容：** 也可以直接输入数字 ID
```
25
```

---

### 3. 修改城市 (菜单选项 3)

**JSON 格式：**
```json
{"id": "25", "name": "新昆明", "x": -1250, "y": -149, "description": "更新后的简介"}
```

**注意：** 必须提供完整的城市信息（包含所有字段）

---

### 4. 添加线路 (菜单选项 4)

**JSON 格式：**
```json
{"source": "1", "target": "33"}
```

**字段说明：**
- `source`: 起点城市编号（必需，字符串格式）
- `target`: 终点城市编号（必需，字符串格式）

**系统行为：**
- 自动计算两城市间的欧几里得距离
- 验证城市是否存在
- 显示添加成功的线路信息

**示例：**
```json
{"source": "1", "target": "2"}
{"source": "101", "target": "102"}
```

---

### 5. 删除线路 (菜单选项 5)

**JSON 格式：**
```json
{"source": "1", "target": "33"}
```

---

## 错误处理

### 常见错误及解决方法

1. **JSON格式错误**
   ```
   ✗ JSON格式错误！请检查输入格式。
   ```
   - 检查是否缺少引号、逗号或大括号
   - 确保字段名使用双引号

2. **缺少必需字段**
   ```
   ✗ JSON格式错误！请检查输入格式。
     必需字段: id, name, x, y
   ```
   - 添加城市时必须提供所有必需字段

3. **城市不存在**
   ```
   ✗ 城市不存在！
   ```
   - 检查城市 ID 是否正确
   - 使用菜单选项 6 查看所有城市

4. **添加线路失败**
   ```
   ✗ 添加失败，请检查城市编号！
   ```
   - 确认起点和终点城市都已存在

---

## 数据文件格式

### 保存格式
系统自动以 JSON 格式保存数据到：
- `data/cities.json` - 城市数据
- `data/routes.json` - 线路数据

### 城市文件示例 (cities.json)
```json
[
  {
    "id": "1",
    "name": "北京",
    "x": 0,
    "y": 0,
    "description": "首都"
  },
  {
    "id": "2",
    "name": "上海",
    "x": 500,
    "y": 300,
    "description": "经济中心"
  }
]
```

### 线路文件示例 (routes.json)
```json
[
  {
    "id": "r1",
    "source": "1",
    "target": "2"
  },
  {
    "id": "r2",
    "source": "2",
    "target": "3"
  }
]
```

---

## 使用技巧

1. **复制粘贴**：可以预先准备好 JSON 字符串，直接粘贴到命令行

2. **批量操作**：虽然当前不支持 JSON 数组批量输入，但可以通过脚本循环调用

3. **数据验证**：输入后系统会显示解析结果，便于确认

4. **向后兼容**：删除城市功能仍支持直接输入数字 ID

---

## 编译说明

使用以下命令编译支持 JSON 输入的版本：

```bash
g++ -std=c++11 -O2 src/main.cpp src/graph/Graph.cpp src/algorithms/*.cpp src/io/FileIO.cpp src/ui/CLI.cpp src/ApiServer.cpp src/SimpleHttpServer.cpp -o network_json.exe -lws2_32
```

---

## 注意事项

1. **字符编码**：确保控制台支持 UTF-8 编码以正确显示中文
2. **引号使用**：JSON 中所有字符串必须使用双引号 `"`，不能使用单引号 `'`
3. **数字格式**：ID 字段虽然是数字，但在 JSON 中以字符串形式输入（带引号）
4. **坐标系统**：Y 坐标在保存时会自动取反（适配前端显示）

---

## 快速参考

| 操作 | JSON 格式 |
|------|-----------|
| 添加城市 | `{"id": "1", "name": "北京", "x": 0, "y": 0, "description": "首都"}` |
| 删除城市 | `{"id": "1"}` 或直接输入 `1` |
| 修改城市 | `{"id": "1", "name": "新北京", "x": 0, "y": 0, "description": "更新"}` |
| 添加线路 | `{"source": "1", "target": "2"}` |
| 删除线路 | `{"source": "1", "target": "2"}` |
